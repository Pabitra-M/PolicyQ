{
  "name": "project-root",
  "scripts": {
    "start": "concurrently -k -n frontend,backend -c blue,green \"npm run dev --prefix frontend\" \"cd backend && nodemon index.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
